name: CI

on:
  push:
    paths:
      - 'AI/DotGuide_CEP/js/**'
      - 'AI/DotGuide_CEP/test/**'
      - 'AI/DotGuide_CEP/package.json'
      - 'AI/DotGuide_CEP/.eslintrc.json'
  pull_request:
    paths:
      - 'AI/DotGuide_CEP/js/**'
      - 'AI/DotGuide_CEP/test/**'
      - 'AI/DotGuide_CEP/package.json'
      - 'AI/DotGuide_CEP/.eslintrc.json'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AI/DotGuide_CEP
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm run lint:check

  test:
    name: Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AI/DotGuide_CEP
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
